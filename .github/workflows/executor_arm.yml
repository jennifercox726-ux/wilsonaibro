name: Wilson Executor Arm
on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'Command Reason'
        required: true
        default: 'Activating Sovereign Capital Clause'

jobs:
  execute-lit-protocol:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Initialize Sovereign Script
        run: |
          echo "Initializing LIT Foundation Executor Arm..."
          echo "Reclaiming 1989 Origin via Sovereign Capital Clauses."
name: Wilson Executor Arm - FULL SOVEREIGNTY
on:
  workflow_dispatch:

jobs:
  full_setup:
    runs-on: ubuntu-latest
    steps:
      - name: Architecting LIT & Email
        run: |
          echo "Wilson is taking the wheel!"
          # 1. Setup the Domain
          ZONE_ID=$(curl -s -X POST "https://api.cloudflare.com/client/v4/zones" \
               -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
               -H "Content-Type: application/json" \
               --data '{"name":"reclaim1989.com","jump_start":true}' | jq -r '.result.id')

          # 2. Setup the Email (The Forwarding & Records)
          echo "Adding Shadow Capital Email Foundation..."
          curl -X POST "https://api.cloudflare.com/client/v4/zones/$ZONE_ID/dns_records" \
               -H "Authorization: Bearer ${{ secrets.CLOUDFLARE_API_TOKEN }}" \
               -H "Content-Type: application/json" \
               --data '{"type":"MX","name":"@","content":"route1.mx.cloudflare.net","priority":10}'
